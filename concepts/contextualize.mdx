---
title: 'Contextualize'
description: 'Is aggegration and enriching the data with historic data or other streams of context.'
---

During this phase entity as recognized, the data is multi-plexed across different sources while maintaining timestamps/order of events.


## Testing and Eval

You can then eval the result from different models.

For example say you want to build a word count app.

You have a pipeline that describes the steps. 

For each case for each pipeline, there is an outcome that you want to eval.


